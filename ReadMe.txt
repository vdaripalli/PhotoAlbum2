The executable folder has PhotoAlbum.exe and all the required dll's to run the application.

Usage:

open command prompt switch to executable folder and run the following commands:

>PhotoAlbum (Return all the photos in all albums)

>PhotoAlbum 1 (Returns only photos for the specified album)